# 14 节点配置对话框国际化

## 概述

解决 `node_config_dialog.py` 中 20 处硬编码中英文字符串问题，完成 Resource Limits 区域和提示信息的 i18n 迁移。

---

## 一、修改清单

| 文件 | 改动 |
|------|------|
| `strings_cn.json` | +19 中文键 |
| `strings_en.json` | +19 英文键 |
| `translation_keys.py` | +19 类属性 |
| `node_config_dialog.py` | 20 处 `"..."` → `t(TK.KEY)` |

---

## 二、替换对照（中文模式）

| 修改前 | 修改后 |
|------|------|
| `"Resource Limits"` | `"资源限制"` |
| `"Priority:" / "Low" / "Normal"` | `"优先级:" / "低" / "正常"` |
| `"CPU Limit (%):"` | `"CPU 限制(%):"` |
| `"Memory Limit:"` | `"内存限制:"` |
| `"Unlimited"` | `"无限制"` |
| `"Apply Resource Limits"` | `"应用资源限制"` |
| tooltip `"100 = 1 core..."` | `"100 = 1 核, 200 = 2 核..."` |
| `"Applied: priority=low, cpu=200%"` | `"已应用: 优先级=低, cpu=200%"` |
| `"Resource limits cleared..."` | `"资源限制已清除（全部默认值）"` |

---

## 三、测试状态

193/193 测试通过，ruff check 全绿。
