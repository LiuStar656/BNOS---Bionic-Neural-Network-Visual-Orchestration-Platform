# 🔄 节点状态同步与项目持久化完善

## 🔄 节点状态同步与项目持久化完善 (2026-06-07)

### 已修复的问题

**1. 节点状态信息未正常更新**
- **问题**：画布节点的CPU、内存信息未正常更新，与资源监测面板显示不一致
- **原因**：节点状态获取方式存在冲突，
ode_monitor 模块与资源监测面板数据来源不同
- **修复**：
  - 废弃 ui/core/node_monitor.py 模块
  - 资源监测面板新增 
ode_state_updated 信号，转发节点CPU、内存数据
  - 画布节点通过信号接收资源监测面板的数据，确保数据源一致
- **修改文件**：
  - ui/panels/resource_monitor.py - 新增信号和数据转发
  - ui/panels/resource_monitor_dock.py - 新增信号和数据转发
  - ui/canvas/items/node_item.py - 移除 
ode_monitor 依赖，改用信号接收数据

**2. 异步调用导致数据加载不及时**
- **问题**：节点创建后无法接收资源监测面板的数据信号
- **原因**：信号连接时序问题，资源监测面板创建晚于节点
- **修复**：主窗口新增 _connect_existing_nodes_to_resource_monitor() 方法，面板创建后主动连接已存在节点的信号
- **修改文件**：ui/main_window.py

**3. 节点运行时间未正常更新**
- **问题**：节点运行时间显示为 0s
- **原因**：未记录节点启动时间
- **修复**：
  - 节点新增 _start_time 属性
  - 当节点状态变为 'running' 或 'idle' 时记录启动时间
  - 实时计算并更新运行时长
- **修改文件**：ui/canvas/items/node_item.py

**4. 节点UI布局调整**
- **问题**：节点内部元素布局不合理，名称、状态指示灯、语言标签位置不符合要求
- **修复**：
  - 节点高度调整为 120px
  - 节点名称居中显示在正上方
  - 状态指示灯与节点名称平行排列
  - CPU、内存、运行时间靠左从上到下排列
  - 语言标签（Python）顶边与节点底边相切
  - 运行时间居中显示
- **修改文件**：ui/canvas/items/node_item.py

**5. 项目持久化功能完善**
- **问题**：重启GUI后需要手动打开项目
- **修复**：
  - 打开项目时记录到 pp_config.json
  - 关闭GUI时再次记录已打开的项目
  - 下次打开GUI时自动加载上次打开的项目
- **修改文件**：
  - ui/main_window.py - 自动打开项目逻辑
  - ui/core/project_manager.py - 记录项目路径

**6. 删除废弃模块**
- **删除文件**：ui/core/node_monitor.py

### 技术实现要点

**信号转发机制**
`python
# resource_monitor.py - 资源监测面板发送信号
node_state_updated = pyqtSignal(str, float, float)  # node_name, cpu_percent, memory_mb

def _update_single_node_stats(self, node_name, node_info):
    # 计算CPU、内存
    cpu_percent = ...
    memory_mb = ...
    self.node_state_updated.emit(node_name, cpu_percent, memory_mb)
`

**节点接收数据**
`python
# node_item.py - 节点接收信号并更新显示
def _on_status_updated(self, node_name, cpu_percent, mem_mb):
    if node_name != self.node_name:
        return
    duration_seconds = 0
    if self._start_time:
        duration_seconds = (datetime.now() - self._start_time).total_seconds()
    self._status_widget.update_status(cpu_percent, mem_mb, duration_seconds)
`

**启动时间记录**
`python
def _try_initialize_start_time(self):
    if self.node_name in self.canvas.parent_window.nodes_data:
        node_info = self.canvas.parent_window.nodes_data[self.node_name]
        if node_info.get('status') in ('running', 'idle'):
            self._start_time = datetime.now()
`

**项目持久化**
`python
def auto_open_last_project(self):
    last_project = self.app_config.get('last_project')
    if last_project and os.path.exists(last_project):
        QTimer.singleShot(200, lambda: self._auto_open_project(last_project))
`

### 验收标准

✅ 画布节点CPU、内存信息与资源监测面板显示一致
✅ 节点状态实时更新，无延迟
✅ 节点运行时间正确显示并实时更新
✅ 节点UI布局符合要求：名称在正上方、语言标签在底边、运行时间居中
✅ 重启GUI后自动打开上次打开的项目
✅ 所有功能正常工作，无错误
✅ 
ode_monitor.py 已删除
✅ 代码结构清晰，易于维护

---

---