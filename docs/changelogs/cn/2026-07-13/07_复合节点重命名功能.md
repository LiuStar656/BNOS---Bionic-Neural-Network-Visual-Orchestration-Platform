# 07 复合节点重命名功能

## 问题描述

复合节点在节点列表中只显示 hex ID，无法自定义展示名称，且没有重命名功能。

## 修复方案

### 7.1 节点列表右键菜单添加重命名选项

**文件**：`ui/panels/node_list_context.py`

在复合节点右键菜单中添加「重命名复合节点」选项。

**代码变更**：
```python
rename_action = menu.addAction("重命名复合节点")
rename_action.triggered.connect(lambda: self._rename_composite_group(group_name))
```

### 7.2 新增重命名方法

**文件**：`ui/panels/node_list_context.py`

新增 `_rename_composite_group()` 方法：
1. 运行态保护：运行中的复合节点禁止重命名
2. 通过输入框编辑 `display_name`
3. 留空恢复 hex ID 显示
4. 刷新节点列表

**代码变更**：
```python
def _rename_composite_group(self, group_name):
    comp_id = group_name[len(CompositeNode.GROUP_PREFIX):]
    if mgr.is_running(comp_id):
        parent.show_toast("复合节点正在运行中，请先停止后再重命名", "warning")
        return
    current_name = mgr._composites.get(comp_id, {}).get("display_name", "")
    new_name = themed_input(self, "重命名复合节点", "...", current_name)
    if new_name is None:
        return
    mgr.rename(comp_id, new_name)
    self.update_node_list(self.nodes_data)
```

## 修改文件清单

| 文件 | 改动 |
|------|------|
| `ui/panels/node_list_context.py` | 新增 `_rename_composite_group()` 方法；右键菜单添加重命名选项 |

## 效果验证

修复后：
- ✅ 节点列表右键菜单支持重命名复合节点
- ✅ 运行中的复合节点禁止重命名
- ✅ 可自定义展示名称，留空恢复 hex ID 显示
- ✅ 重命名后节点列表自动刷新

---

**最后更新**：2026-07-13
