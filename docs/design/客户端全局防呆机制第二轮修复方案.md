# 客户端全局防呆机制 — 第二轮修复方案

> 覆盖第一轮未涉及的 7 个新文件，共 9 项问题。

---

## 一、改动清单

| 编号 | 文件 | 问题 | 优先级 | 改动量 |
|------|------|------|--------|--------|
| R01 | `property_panel.py` | `start_script` UnboundLocalError | P0 | +3 |
| R02 | `bnos_console.py` | 重启 Popen 无异常保护 | P0 | +5 |
| R03 | `app_state.py` | `_notify()` 迭代无锁 | P1 | +3 |
| R04 | `file_operation_manager.py` | `_is_running` check-then-act 竞态 | P1 | +12 |
| R05 | `edge_item.py` | `float()` 反序列化无保护 | P1 | +6 |
| R06 | `node_list_ops.py` | 3 处 `data['name']` → `.get()` | P1 | +3 |
| R07 | `node_list_context.py` | 3 处 `data['name']` → `.get()` | P1 | +3 |
| R08 | `node_list_drag.py` | 3 处 `data['name']` → `.get()` | P1 | +3 |
| R09 | `property_panel.py` | `start_path` 可能为 None | P2 | +2 |

---

## 二、各机制详细设计

### R01 — UnboundLocalError

**文件**: `property_panel.py:175-182`

```python
# 修复: 在 if-elif-else 链之前初始化 start_script
start_script = "<unknown>"
if os.path.exists(os.path.join(start_path, "start.bat")):
    start_script = os.path.join(start_path, "start.bat")
elif os.path.exists(os.path.join(start_path, "start.sh")):
    start_script = os.path.join(start_path, "start.sh")
else:
    themed_message(self, t("k_title_error"), t("_k_start_script_missing").format(script=start_script), "error")
```

### R02 — bnos_console Popen 保护

**文件**: `bnos_console.py:100-109`

```python
if ret == 42:
    try:
        import subprocess
        subprocess.Popen([sys.executable, restart_script, main_script] + args,
                        cwd=os.getcwd(), close_fds=True)
    except Exception as e:
        logger.error("重启子进程启动失败: %s", e)
        sys.exit(1)
sys.exit(ret)
```

### R03 — app_state 通知锁

**文件**: `app_state.py:243`

```python
def _notify(self, path, new_value, old_value):
    with self._lock:
        matched_paths = [sp for sp in self._subscribers.keys()
                        if sp == path or (sp.endswith('.*') and path.startswith(sp[:-2])) or sp == '*']
    ...
```

### R04 — file_operation_manager 竞态

**文件**: `file_operation_manager.py:22,28-32`

```python
def __init__(self):
    self._is_running = False
    self._cancel_event = threading.Event()
    self._lock = threading.Lock()      # 新增

def copy_file(self, source_path, dest_path):
    with self._lock:
        if self._is_running:
            self.operation_error.emit('copy', 'Another operation is running')
            return
        self._is_running = True
```

在 finally 中也加锁:
```python
finally:
    with self._lock:
        self._is_running = False
```

### R05 — edge_item float 保护

**文件**: `edge_item.py:710`

```python
wps = []
for r in raw:
    try:
        wps.append((float(r[0]), float(r[1]), float(r[2])))
    except (ValueError, TypeError, IndexError):
        logger.warning("跳过无效路点数据: %s", r)
self._waypoints = wps
```

### R06/R07/R08 — data['name'] → data.get('name')

3 个文件共 9 处统一改为 `.get('name', '')`。

### R09 — property_panel start_path None

```python
if start_path and os.path.exists(start_path):
```

新增的 `start_path` 直接 None 检查。

---

## 三、改动量估算

| 文件 | 改动行数 |
|------|----------|
| `property_panel.py` | +5 |
| `bnos_console.py` | +5 |
| `app_state.py` | +3 |
| `file_operation_manager.py` | +12 |
| `edge_item.py` | +6 |
| `node_list_ops.py` | +3 |
| `node_list_context.py` | +3 |
| `node_list_drag.py` | +3 |
| **合计** | **~40** |
