# 面板加载顺序调整

**日期：** 2026-06-12  
**修改人员：** AI Assistant

---

## 修改目的

调整 CanvasHost 中面板的加载顺序，确保**画布面板先于终端面板加载**。

## 修改内容

### 1. CanvasHost 初始化调整

**文件：** `ui/core/canvas_host.py`

#### 修改前
```python
# 初始化时显示空白缓冲层，不创建任何画布
self._init_blank_placeholder()

# 设置样式
self._setup_styles()

# 初始化终端 Dock
self._init_terminal_dock()
```

#### 修改后
```python
# 初始化时显示空白缓冲层，不创建任何画布
self._init_blank_placeholder()

# 设置样式
self._setup_styles()

# 标记终端是否已初始化
self._terminal_initialized = False
# 终端 Dock 将在第一个画布创建后初始化
```

### 2. 终端延迟初始化

**文件：** `ui/core/canvas_host.py`

#### 新增方法
```python
def _ensure_terminal_initialized(self):
    """确保终端已初始化（仅第一次调用时真正初始化）"""
    if self._terminal_initialized:
        return
    self._init_terminal_dock()
```

#### 修改 `_init_terminal_dock`
```python
def _init_terminal_dock(self):
    """初始化终端 Dock，停靠在底部区域"""
    if self._terminal_initialized:
        return
        
    self._terminal_dock = TerminalDock(self, self._parent_window)
    # ... 原有代码 ...
    
    self._terminal_initialized = True
    logger.info("CanvasHost: 终端 Dock 已初始化")
```

### 3. 画布创建时初始化终端

**文件：** `ui/core/canvas_host.py`

在 `_create_canvas_dock` 方法中：

```python
# 9. 设置为活动画布
self._active_canvas = canvas

# 10. 如果是第一个画布，初始化终端 Dock（确保画布先加载）
if len(self._canvas_docks) == 1:
    self._init_terminal_dock()

# 11. 连接信号 - 监听关闭事件和焦点变化事件
# ...
```

### 4. toggle_terminal 方法调整

**文件：** `ui/core/canvas_host.py`

```python
def toggle_terminal(self):
    """切换终端 Dock 的显示/隐藏"""
    # 确保终端已初始化
    self._ensure_terminal_initialized()
    
    if self._terminal_dock.isVisible():
        self._terminal_dock.hide()
    else:
        self._terminal_dock.show()
```

### 5. 主窗口终端恢复逻辑调整

**文件：** `ui/main_window/state.py`

```python
def _restore_terminal_dock(self):
    """恢复终端 Dock 的可见性
    
    注意：仅在终端已初始化（画布已创建）时才恢复可见性。
    如果终端尚未初始化，则跳过 —— 它将在第一个画布创建时自动初始化。
    """
    visibility = self.app_config.get("panel_visibility", {})
    show_terminal = visibility.get("terminal_dock", True)
    
    if hasattr(self, '_canvas_host') and self._canvas_host:
        ch = self._canvas_host
        if hasattr(ch, '_terminal_dock') and ch._terminal_dock:
            if show_terminal:
                ch._terminal_dock.show()
            else:
                ch._terminal_dock.hide()
```

---

## 加载顺序

### 新的加载流程

```
1. CanvasHost.__init__()
   ├── _init_blank_placeholder()  # 显示空白缓冲层
   ├── _setup_styles()            # 设置样式
   └── _terminal_initialized = False  # 标记终端未初始化

2. 用户打开项目 / 创建新画布
   └── _create_canvas_dock()
       ├── _remove_blank_placeholder()  # 移除缓冲层
       ├── 创建画布 Dock
       ├── 设置为活动画布
       ├── 【关键】_init_terminal_dock()  # 第一个画布创建时初始化终端
       └── 连接信号

3. 终端初始化
   └── _init_terminal_dock()
       ├── 创建 TerminalDock
       ├── 停靠在底部
       ├── 默认隐藏
       └── _terminal_initialized = True
```

### 场景处理

| 场景 | 行为 |
|------|------|
| **启动应用（无项目）** | 只显示空白缓冲层，不加载终端 |
| **打开项目** | 先创建画布，再初始化终端 |
| **新建画布** | 如果是第一个画布，同时初始化终端 |
| **切换终端** | 如果终端未初始化，先初始化再切换 |
| **关闭所有画布** | 回到空白缓冲层，终端保持（如果已初始化）|

---

## 验证结果

- ✅ CanvasHost 导入成功
- ✅ MainWindowStateMixin 导入成功
- ✅ 代码语法正确
- ✅ 加载顺序符合预期

---

## 注意事项

1. **终端初始化时机**：终端仅在第一个画布创建时初始化，避免资源浪费
2. **向后兼容**：`toggle_terminal()` 会自动处理未初始化的情况
3. **状态恢复**：`_restore_terminal_dock()` 只在终端已存在时恢复状态
4. **性能优化**：避免了启动时立即创建终端的开销

---

**修改完成**
