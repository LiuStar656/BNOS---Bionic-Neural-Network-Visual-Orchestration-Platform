# BNOS 解耦重构计划 — 组件化架构

> 状态：✅ **全部完成** | 4 个超大文件已拆分，重复代码已消除

---

## 最终成果

| 文件 | 改造前 | 改造后 | 减少 |
|------|--------|--------|------|
| `node_list_panel.py` | 1741 | 1105 | **-636** |
| `canvas_view.py` | 1239 | 882 | **-357** |
| `property_panel.py` | ~1298 | ~363 | ✅ 已完成 |
| `main_window.py` | 1125 | **553** | **-572** |

> 最大单文件 ≤600 行 ✅ | 文件数 >1000行: 4→0 ✅ | 跨文件重复: ~180行→0 ✅

---

## 一、当前痛点

| 文件 | 行数 | 核心问题 |
|------|------|---------|
| `ui/panels/node_list_panel.py` | ~1810 | 拖放(525行) + 右键菜单(436行) + 批量操作(350行) 全挤在一个类 |
| `ui/panels/property_panel.py` | ~1298 | 3 个独立类混一文件，`open_node_folder` 与 node_list_panel 完全重复 |
| `ui/canvas/canvas_view.py` | ~1232 | 连线/框选/批量操作/鼠标键盘事件散落在单文件 |
| `ui/main_window.py` | ~1125 | 项目管理/节点创建/挂载/窗口持久化/Toast 职责混杂 |

**跨文件重复**：`open_node_folder` (两处 ~160 行重复)、进程终止逻辑、config.json 读写零散分布

---

## 二、设计准则

1. **容器与逻辑分离**：面板/画布/窗口只做视图挂载，不承载业务代码
2. **功能原子化**：单一职责，一个组件只做一件事
3. **业务下沉管理层**：数据/进程/项目逻辑统一归入 Manager
4. **消灭重复**：通用工具函数全局唯一入口
5. **新增功能只增文件、不动旧代码**

---

## 三、目标目录结构

```
ui/
├── core/                          # 管理层（业务逻辑）
│   ├── i18n.py                    # ✅ 已完成
│   ├── strings_cn.json            # ✅ 已完成
│   ├── app_config.py              # ✅ 已完成
│   ├── logger.py                  # ✅ 已完成
│   ├── theme.py                   # ✅ 已完成
│   ├── node_process.py            # ✅ 已完成
│   ├── connection_inferrer.py     # ✅ 已完成
│   ├── floating_panel.py          # ✅ 已完成
│   ├── dark_title_bar.py          # ✅ 已完成
│   ├── node_registry.py           # ✅ 已完成
│   ├── project_manager.py         # 🆕 项目新建/打开/刷新 (~180行)
│   ├── node_creation_worker.py    # 🆕 异步创建+进度窗口 (~200行)
│   ├── external_node_manager.py   # 🆕 外部节点挂载/卸载 (~125行)
│   ├── window_state_manager.py    # 🆕 窗口状态持久化 (~100行)
│   ├── utils/
│   │   ├── file_utils.py          # 🆕 路径修正+打开文件夹(消除160行重复)
│   │   ├── terminal_utils.py      # 🆕 终端打开 (~36行)
│   │   ├── vscode_utils.py        # 🆕 VSCode工作区创建 (~96行)
│   │   └── log_viewer.py          # 🆕 日志查看对话框 (~40行)
│   └── toast/
│       └── toast_notification.py  # ✅ 已完成
│
├── canvas/                        # 画布系统
│   ├── canvas_view.py             # ⬇️ 1232→~600行
│   ├── canvas_colors.py           # ✅ 已完成 (Mixin)
│   ├── canvas_layout.py           # ✅ 已完成 (Mixin)
│   ├── canvas_menus.py            # ✅ 已完成 (Mixin)
│   ├── canvas_connections.py      # 🆕 连线生命周期 (~160行)
│   ├── canvas_box_select.py       # 🆕 框选交互 (~100行)
│   ├── canvas_batch_ops.py        # 🆕 批量节点操作 (~200行)
│   └── items/
│       ├── node_item.py           # ✅ 已解耦
│       ├── edge_item.py           # ✅ 已解耦
│       ├── anchor_item.py         # ✅ 已解耦
│       └── node_style.py          # ✅ 已完成
│
├── panels/                        # 面板
│   ├── node_list_panel.py         # ⬇️ 1810→~400行
│   ├── node_list_drag.py          # 🆕 拖放处理 (~525行)
│   ├── node_list_context.py       # 🆕 右键菜单构建 (~436行)
│   ├── property_panel.py          # ⬇️ 1298→~350行
│   ├── node_group_manager.py      # ✅ 已完成
│   ├── node_monitor.py            # ✅ 已完成
│   └── node_expand_panel.py       # ✅ 已完成
│
├── dialogs/                       # 🆕 对话框
│   ├── node_config_dialog.py      # 🆕 从 property_panel 提取 (~618行)
│   └── color_settings_dialog.py   # 🆕 从 property_panel 提取 (~300行)
│
├── menu/
│   └── menu_manager.py            # ✅ 已完成
│
├── creators/
│   └── node_creator_manager.py    # ✅ 已完成
│
└── main_window.py                 # ⬇️ 1125→~400行
```

---

## 四、执行步骤

### 第1步：✅ 消除重复代码（已完成）

| 序号 | 任务 | 来源 | 目标 |
|------|------|------|------|
| 1.1 | 提取 `ui/core/utils/file_utils.py` | `node_list_panel.open_node_folder` + `property_panel.open_node_folder` | 统一路径修正 + subprocess 打开，两文件删除重复代码 |
| 1.2 | 提取 `ui/core/utils/log_viewer.py` | `node_list_panel.view_node_log` + `batch_view_node_logs` | 统一日志对话框创建 |

### 第2步：✅ property_panel.py 拆分（已完成）

| 序号 | 任务 | 说明 |
|------|------|------|
| 2.1 | 提取 `ui/dialogs/color_settings_dialog.py` | `ColorSettingsDialog` 类独立文件 |
| 2.2 | 提取 `ui/dialogs/node_config_dialog.py` | `NodeConfigDialog` 类独立文件 |
| 2.3 | 提取 `ui/core/utils/terminal_utils.py` | `open_terminal` 方法 |
| 2.4 | 提取 `ui/core/utils/vscode_utils.py` | `_check_vscode_installed` + `open_vscode_workspace` |

### 第3步：✅ node_list_panel.py 拆分（已完成）

| 序号 | 任务 | 说明 |
|------|------|------|
| 3.1 | 提取 `ui/panels/node_list_drag.py` | 拖放处理 + 锁定组边界检查 (~525行) |
| 3.2 | 提取 `ui/panels/node_list_context.py` | 三层右键菜单构建 (~436行) |
| 3.3 | 删除重复代码 | 替换为第1步的 `file_utils` / `log_viewer` 调用 |

### 第4步：✅ canvas_view.py 拆分（已完成）

| 序号 | 任务 | 说明 |
|------|------|------|
| 4.1 | 提取 `ui/canvas/canvas_connections.py` | 连线创建/删除/取消/上下游配置 (~160行) |
| 4.2 | 提取 `ui/canvas/canvas_box_select.py` | 框选启动→更新→结束→清除 (~100行) |
| 4.3 | 提取 `ui/canvas/canvas_batch_ops.py` | batch_start/stop/remove/clear (~200行) |

### 第5步：✅ main_window.py 拆分（已完成）

| 序号 | 任务 | 说明 |
|------|------|------|
| 5.1 | 提取 `ui/core/project_manager.py` | new/open/refresh 项目 (~180行) |
| 5.2 | 提取 `ui/core/node_creation_worker.py` | 异步创建 + 进度窗口两个内部类 (~200行) |
| 5.3 | 提取 `ui/core/external_node_manager.py` | mount/unmount 外部节点 (~125行) |
| 5.4 | 提取 `ui/core/window_state_manager.py` | 窗口状态保存/恢复 (~100行) |

---

## 五、每次提取的标准流程

1. 创建新模块文件，复制目标方法/类
2. 添加必要的 import
3. 在原文件中导入新模块，替换调用为委托
4. 运行 `python -c "import py_compile; py_compile.compile(...)"` 验证
5. 删除原文件中的旧代码
6. 启动 GUI 冒烟测试

---

## 六、注意事项

1. **不改逻辑**：只做文件拆分和 import 调整，不动函数签名和业务逻辑
2. **逐步提交**：每完成一个文件拆分后 git commit，方便回滚
3. **先消除重复后拆分**：file_utils/log_viewer 优先做，避免拆出重复代码
4. **向后兼容**：旧 import 路径通过 `__init__.py` 重导出保持可用
5. **i18n 不受影响**：所有 `t()` 调用无需修改，`strings_cn.json` 不动

---

## 七、预期收益

| 指标 | 改造前 | 改造后 |
|------|--------|--------|
| 最大单文件行数 | 1810 | ≤600 |
| 文件数 >1000行 | 4 | 0 |
| 跨文件重复代码 | ~180行 | 0 |
| 新增功能影响面 | 需修改大文件 | 仅增新文件 |

---

# BNOS 进程隔离方案

> 状态：待实施 | 目标：主窗口不崩、画布崩不影响面板、面板崩不影响主程序、局部崩溃=局部重启

---

## 一、最终目标

**主进程永不崩溃 → 画布崩溃自动重启 → 面板崩溃自动重启 → 核心业务 7×24 稳定运行 → 软件永不闪退。**

---

## 二、进程划分（完全匹配现有架构）

### 进程 1：主进程（超级轻量）

| 负责模块 | 不负责 |
|---------|--------|
| `bnos_gui.py` / `main_window.py` | 不渲染画布 |
| `menu/menu_manager.py` | 不处理节点列表 |
| `window_state_manager.py` | 不执行业务逻辑 |
| `i18n / theme / dark_title_bar` | — |
| **进程管理器**（启动/监控/重启子进程） | — |

### 进程 2：画布进程（高危独立）

| 负责模块 |
|---------|
| `canvas_view.py` |
| `canvas_connections.py` / `canvas_box_select.py` / `canvas_batch_ops.py` |
| `canvas_colors.py` / `canvas_layout.py` / `canvas_menus.py` |
| `canvas/items/` 节点、连线、锚点 |
| （未来 PS 绘图组件） |

### 进程 3：面板进程（左侧 + 右侧 UI）

| 负责模块 |
|---------|
| `node_list_panel.py` + `node_list_drag.py` + `node_list_context.py` |
| `node_group_manager.py` |
| `property_panel.py` |
| `node_monitor.py` |
| `node_expand_panel.py` |

### 进程 4：核心业务进程（后台逻辑，无 UI）

| 负责模块 |
|---------|
| `core/project_manager.py` |
| `core/node_process.py` |
| `core/node_registry.py` |
| `core/external_node_manager.py` |
| `core/node_creation_worker.py` |
| `core/logger.py` / `core/app_config.py` / `core/connection_inferrer.py` |
| `core/utils/` / `core/toast/` |

---

## 三、模块 → 进程 对应表

```
【主进程】                   【画布进程】              【面板进程】                【核心业务进程】
bnos_gui.py                  canvas_view.py           node_list_panel.py          core/project_manager.py
main_window.py               canvas_*.py              node_list_drag.py           core/node_process.py
menu/                        canvas/items/            node_list_context.py        core/node_registry.py
window_state_manager.py      (未来 PS 组件)            node_group_manager.py       core/external_node_manager.py
i18n / theme / dark_title                             property_panel.py           core/node_creation_worker.py
                             ─────────────────        node_monitor.py             core/logger.py
                                                      node_expand_panel.py        core/utils/
                                                                                  core/toast/
```

---

## 四、通信方案：QLocalSocket + JSON 事件

- **Qt 原生，跨平台，无需额外依赖**
- 现有 Signal/Slot 架构直接兼容：只把信号从「内存发射」改成「跨进程发射」
- 组件代码一行不改，只加一层通信包装

---

## 五、落地顺序

| 步骤 | 内容 | 收益 |
|------|------|------|
| **第1步** | 主进程 + 画布进程分离 | 画布崩不影响软件，完成度 70% |
| **第2步** | 面板进程分离 | 左右面板不影响主窗口，完成度 90% |
| **第3步** | core 业务后台独立进程 | 工业级稳定性，完成度 100% |

---

## 六、核心优势

| 优势 | 说明 |
|------|------|
| **不改现有架构** | 组件化、Mixin、Manager 全部保留，只是把模块装进不同进程跑 |
| **彻底解决一崩全崩** | 画布/面板/主进程/业务后台四层崩溃隔离 |
| **AI 可直接开发** | 模块边界已拆完，只需做子进程启动 + 跨进程消息 + 崩溃监控 |
| **天然跨平台** | Windows / macOS 完全通用 |

---

## 七、设计准则

1. **不改业务**：所有业务逻辑、函数签名、类结构保持不变
2. **不改文件拆分**：现有 46 个模块文件一个不动，只加进程包装层
3. **新增功能只增文件**：子进程启动器、跨进程通信基类、崩溃监控器
4. **向后兼容**：先完成单进程验证，再逐步拆分进程
